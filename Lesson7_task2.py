# Ввод строки
s = input("Введите строку: ")

# Проверка длины строки
if len(s) > 1000:
    print("Ошибка: длина строки превышает 1000 символов.")
else:
    # Преобразование строки: удаление лишних пробелов
    result = " ".join(s.split())

    # Вывод результата
    print(result)



    
